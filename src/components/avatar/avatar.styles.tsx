import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderRadius: 100,
    borderColor: 'red',
    borderWidth: 1,
    width: 150,
    height: 150,
    justifyContent: 'center',
    alignItems: 'center',
  },
  loader: {
    flex: 1,
  },
});
