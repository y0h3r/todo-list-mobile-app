import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    width: '100%',
    borderRadius: 16,
    padding: 4,
    height: 50,
  },
  label: {
    color: '#fff',
    fontSize: 16,
    alignSelf: 'center',
  },
});
